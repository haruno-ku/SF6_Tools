# 診断ファイルの由来

**誰が入力したかは JSON に書かれていない。** ここに書く。

環境: SF6 build 24176760（exe 2026-08-07） / REFramework v1.5.9.1 /
Zangief(ESF_006) Modern vs Luke(ESF_002) / 中央 / 2026-09-10

| ファイル | 入力者 | 何のためのデータか |
|---|---|---|
| `probe_a_damage-20260910T144239Z.json` | **自動（OSキー入力）** | 参考。7サンプル、2件不一致 |
| `probe_a_damage-20260910T144757Z.json` | **人間** | **これが Probe A の結果。** 20サンプル、18比較可能中3件不一致 |
| `probe_b_clock-20260910T144756Z.json` | 人間 | Probe B。結論あり |
| `probe_c_reset-20260910T144755Z.json` | 人間 | Probe C。結論あり |
| `probe_d_catalog-20260910T145748Z.json` | 自動（OSキー入力） | Probe D、18種類。結論に届かず |
| `probe_d_catalog-20260910T150614Z.json` | 自動 + **人間（SA / OD必殺）** | **これが Probe D の結果。** 25種類、conclusive |

`*-latest.json` は同名の stamped ファイルのコピー。

## 自動入力とは何か、何をしていないか

`0.3.0-diagnostics` は**入力を注入しない**。`Provenance` が未計測のボタンビットを
使う経路を塞いでいて、この実行でもそれは一度も開いていない。

「自動」と書いてあるものは **OS レベルのキーボード入力**（SendInput）で、
人間がキーボードを叩くのと同じ経路。ゲーム自身のキーバインドを通るので
「存在しないボタンを押す」ことは起きない。**フレーム精度は無い。**

その限界がデータに出ている:

- `probe_a_damage-...144239Z` の不一致2件は `hits 1 / field 400 / hpDelta 800`、
  ちょうど2倍。繋がったコンボではなく**短い間隔で入った別々の単発**を
  probe が1サンプルにまとめた形。**ゲームの挙動ではなく入力の質の問題**
- 同じ自動入力でも、1発ずつ間隔を空けた実行では**14件中14件が一致**した

だから Probe A の結果としては**人間の実行（144757Z）を採る**。

Probe B / C / D は「誰が押したか」に依存しない量（フックの発火回数、
リセットの所要時間、出た action_id）なので、自動入力でも同じ値になる。

## Probe D の結果

```
14 of 25 observed ids matched the catalog (20% of catalogued ids seen)
conclusive: true
```

**「14/25 しか一致しなかった」ではない。取りこぼしはゼロである。**

一致しなかった11個（`1` `6` `9` `482` `609` `926` `941` `946` `1016`
`1209` `1219`）は、**全件がカタログ自身の `_meta.unmapped_action_ids`
（291件）に載っている。** カタログが「これらには名前を持たない」と
自分で宣言している id なので、観測されたこと自体が想定内。

つまり **25 件すべてが説明できている**（14 は名前あり、11 は名前が無いと
宣言済み）。RUNBOOK が赤信号と呼ぶ「観測した id が1つもカタログに無い」
とは正反対の結果。

`1209` / `1219` は SA、`926` `941` `946` `1016` は OD必殺の帯にある。

## Step 9 の材料が先に見えている（ただし採用してはいけない）

13ある ambiguous group のうち7つが、実機で1つの id に解決している:

| 表記 | 候補 | 出た | 出なかった |
|---|---|---|---|
| `弱` | 601 / 602 / 611 | **611** | 601, 602 |
| `2 + 弱` | 617 / 618 / 619 | **617** | 618, 619 |
| `DI` | 850 / 855 | **855** | 850 |
| `DP` | 480 / 489 | **480** | 489 |
| `中 + 强` | 900 / 903 | **900** | 903 |
| `弱 + 中 + 强` | 901 / 1497 | **901** | 1497 |
| `2 + SP`（simple） | 900 / 903 | **900** | 903 |

**全件 `canonical_status: unverified` のままであり、そのままにしてある。**
これは自由に遊んだ結果の観測であって、制御された入力ではない。
Step 9 が要求しているのは `{5,2,4,6,1,3} × {L,M,H,SP}` を
ニュートラルから1回ずつ注入する掃引で、これはその代わりにならない。

先に答えが見えていることと、答えが確定していることは別である。
